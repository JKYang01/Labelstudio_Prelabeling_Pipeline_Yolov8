### PDF piece module
THe prelabeling happens here

/base_detection
detect.py contains detection wrapper functions which calls the detection function from detect_base.py
detect_base.py is where the model is placed and do the prediction job

/partial_detection
pic_tile.py if you want to tile the big picture to smaller piece and run pre-annotation, \
this is the scrip which gonna execute the pirecing.



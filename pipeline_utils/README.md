## Pipeline Utils
The tool kit and template placing module.

Future developing notes: 

/cfg 
the template config files folder
inculde: parameter defining yaml file, labelstudio labeling template, keyword filter list, class name list for mapping the prediction results

/utils
module that include tool functions like s3 bucket loading, project creating, formating prediction output.etc. \
any function that will be repetatively used, create a scrip, name if as it's function and write the methods.

